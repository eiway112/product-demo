---
name: product-demo
description: 给任意产品/项目生成「单文件、离线自包含、可交互」的对外演示网页——双击即开、能直接发微信、零外部依赖。用于"演示件""产品展示页""给客户看的介绍页""做个网页讲清楚 X""出个能发给客户的页面""产品官网单页"等需求。自带策划、编排、出件、核验四步；结构 / 叙事 / 交互三层机器判据，每条都配负向注入证明它会失败。
version: 1.8.0
agent_created: true
---

# 通用演示件生成

## 身份（写任何东西前先认这个）

- **判定对象**：某个产品/项目需要对外演示（受众为客户 / 投资人 / 内部）。
- **产出物**：单文件自包含 HTML 演示页——图片 base64 内联、`http` 外链为 0、可交互、离线双击可用。
- **硬约束**：本体内不出现任何具体项目名。品牌、口径、事实数据、素材、交互形态一律外置到 `profiles/<产品>/`。
- **自检法**：产出物描述里冒出具体产品名＝被收窄，改回"某个产品/项目"。
- **结构也会被污染**：跑 `assets/audit_body.py`。它查三族——
  **产品词** / **产品专属字段结构组合** / **交互件模板可见文本零中文（C5）**。
  名字可以一个都不出现，结构照样是焊死的。早期判据只查文本 diff，骨架里留着某产品的
  算例字段组合却报绿，就是漏在这里。
  第一族依赖 `profiles/*/profile.json` 收集词表；干净克隆里没有产品配置，词表必为空、该族退化
  （脚本会明说"无判别力"，不印裸 PASS）。后两族不依赖任何配置，到哪儿都有判别力。

## 调用面

用户只说一句人话（"给 X 出个演示件"），不提技能名、不做选择。刚做过的产品 ≠ 本次的产品，产品名每次都要读到或确认。

## 四步流水线（业务流程）

> **别和「四道机器闸门」混着读**：本节的四步是**做事的顺序**（谁在哪一步产出什么）；
> README 里的「出件 → 核验 → 负向测试 → 本体清洁度」是**四道闸门**（每一步之后机器判什么）。
> 两者同叫「四步」，指的不是同一件事——说「第 3 步」时必须带上哪一套。

### 第一步 · 意图识别与范围锚定

产出**需求卡片**：`产品名称`｜`配置来源`（已有 profile / 新建）｜`受众`｜`形态`｜`素材现状`。

- 受众默认"客户"、形态默认"单页滚动"，未指定时按默认推进并**显式标注"默认假设"**（可逆，不卡流程）。
- profile 缺失 → 走"新建配置"子流程（照 `profiles/_模板/`），**禁止沿用别的产品配置**（串味是最常见的翻车）。
  第一次上手、或想先确认环境能跑通，直接跑 `profiles/_示例/`（全虚构值，见其 `README.md`）。
- 判据 **S1–S4**（人工判）：产品名非空且能定位 profile 或标新建；受众/形态为枚举值；缺省项已标注为"默认假设"；素材现状有结论（够用 / 待补 / 需降采样）。

### 第二步 · 素材准备（策划）★

产出三件套，缺一不可进入下一步：

1. **叙事主线**：一句话主张（≤30 字）+ 3–5 条论据，每条带 `source`（配置键 / 文档路径:行号 / 数据项 key）。
2. **内容骨架**：按叙事五要件组织（见第三步），每件 1–5 个要点。
3. **素材缺口清单**：每项缺口给三选一处置结论（补 / 替换 / 删除）。

- 判据 **P1–P8**（人工判）：主张 ≤30 字、禁用虚词（领先/颠覆/最佳/唯一…除非有权威出处）、**论据可回指率 100%**、槽位三态标记（已实测/待补/推算）、缺口处置率 100%、数值论据必带口径。
- 难点在于"罗列功能、讲不出故事"。判法：删掉一条论据主张仍成立 → 这条不是论据。

### 第三步 · 视觉出件（编排 + 出件）★

三件事：

1. **叙事五要件**（写进 `sections.html`，每段带 `data-role`）：

   | 要件 | `data-role` | 要回答的问题 | 缺件时 |
   |---|---|---|---|
   | 痛点可枚举 | `pain` | 不做会怎样？具体到场景 | 用 `data-role="gap" data-gap-for="pain"` 声明 |
   | 有方法 | `method` | 凭什么能解决 | 同上 |
   | 有实证 | `evidence` | 拿什么证明（交互件通常挂这里） | 同上 |
   | 有差异 | `diff` | 与替代方案差在哪 | 同上 |
   | 有边界 | `boundary` | 做不到什么、什么条件下不适用 | 同上 |

   **缺一件必须显式声明，不许留空**。声明了 gap 的产品，交互件数必须为 0——
   缺数据时硬凑一个空交互件，比没有件更糟（受众点开发现是空的，会直接推断产品没内容）。

2. **交互件**：按 `assets/interaction_patterns.md` 的四类形态选**一件**——
   计算型→档位对照器 / 能力型→样例演示器 / 规格型→配置器 / 流程型→流程沙盘。
   形态由 `profile.json` 的 `interaction.pattern` 声明，数据由 `interaction.data` 指向。
   **归不进四类就不出件**（走 gap 处置），不许临场手写一个专用件塞进本体。

3. **版式预算**（经验值，不是判据；超了先复核再改）：单表 ≤8 行、单行 ≤40 字、
   单区块 ≤120 字、图 ≤5 张、动效仅 fade/位移 ≤400ms、配色字体取自 profile。

- 出件：`python assets/build_demo.py --profile profiles/<产品>/profile.json --out <成品.html>`
  **禁止直接手写含 base64 的超大文件**；内容包缺件时出件器会中止并提示，不会静默出一份空壳。
- 判据 **B1–B8**（结构层）：外链 0、img 数=契约图数且全 `data:`、体积 ≤3MB、UTF-8 无 BOM、headless 无 console error、无本机绝对路径、占位符残留 0、**脚本/样式标签配对且不嵌套**。
  B8 是离线判据里唯一能拦住「交互件静默不执行」的那条——嵌套 `<script>` 会让整个脚本块语法错误、一行都不跑，而源码看上去完全正常。出件器自己也有一道同样的自检。

### 第四步 · 核验与落盘

跑 `python assets/check_demo.py <成品.html> --profile <profile.json>`（零第三方依赖，脚本内不得出现产品名）：

- **N1–N5 叙事层**：五要件覆盖（缺件须有 gap 声明）/ 要件非空壳（≥40 字且含内容块）/
  gap 与交互件互斥 / 层级下限 / 密度分档。
- **I1–I3 交互层**：主输出容器有默认态（初始内容为 `—`）/ `IX_init()` 已定义并调用 /
  内联数据满足 item 契约（≥2 条、字段齐备、区间成对）。对应交互件契约 C1–C4。
  **I2 先剥注释再数**（行注释 `//` 与块注释 `/* */` 都剥，且不误伤字符串字面量）——
  被注释掉的调用长得和真调用一模一样。
  **C5 / C6 不在这里守**——两条判的都是交互件**模板本体**，不是成品：
  C5（模板可见文本零中文）跑 `assets/audit_body.py`；
  C6（模板必须提供 V3 取证锚点 `data-ix-ready`）在调用时用 `--js-value IXRDY` 验证。
  只跑 `check_demo.py` 会漏掉 C5，不给 `--js-value` 则连 C6 也验不到——这是设计如此，不是遗忘。
- **V1–V8 行为层**：外链 0 / 图片全部解码 / JS 已执行 / **关键数值与数据源逐项一致** /
  无横向溢出 / 截图非空 / **交互往返**。
  B5 走自建探针——Chrome 自身的日志把 log/warn/error 一律报成同一级别，分不出错误，
  所以只能在页面内部挂钩子，把 `console.error` 与未捕获异常记进 DOM 再读出来；
  V3 靠模板按契约 C6 写入的 `data-ix-ready` 锚点，调用时传 `--js-value IXRDY`；
  V7 除「非空」外还校验 PNG 头与尺寸，并解出像素判画面非空白——头和尺寸对一张
  全白页全部成立，只看字节数拦不住空白页（纯 stdlib 解 PNG，阈值依据写在函数注释里）。
  **V8 是唯一一条问「点了有没有反应」的判据**：派发一次真实点击，回读 DOM，
  与内联数据源的第 k 项逐项比对。少了它，「卡片索引错位 / 事件绑定失效 / 只更新一半」
  这三类故障在所有静态判据下都是全绿的（V3 只证明初始化跑通了，不证明交互可用）。
  它靠契约 **C7** 取证：卡片必须是 `.ix-card` 且带 `data-ix="<序号>"`，主标题容器
  `id="ix-title"`。模板不满足 C7 时 V8 取不到「点的是哪一张」，只能列 SKIP。
  外链那条同时扫 HTML 属性与 CSS 层的 `url()` / `@import` / `srcset`——只扫属性时，
  一条 `@import url("//…")` 就能让「离线自包含」整条失效而判据仍报绿。
  **缺参数（`--chrome` / `--js-value` / `--shot`）一律列 SKIP，绝不静默消失**：
  「没检查」和「检查过且干净」必须能分开。
- **SKIP ≠ PASS**：判据因缺省条件（如未给 `--chrome`）未执行时单列 SKIP，不计入通过。
- **U1 出席检查**：判据登记在 `check_demo.py` 的 `CRITERIA` 表里，跑完逐条对照，
  没出结论行的一律 FAIL。V3/V7 曾经整段静默消失、V2 在成品无图时也一行不出——
  逐条补是打地鼠，对着登记表查出席才是机制。
- **U2 期望契约**：`--expect pass=n,fail=n,skip=n`。文档里那些数字（"PASS 24 / SKIP 3"）
  此前没有任何东西在看，漂移到 PASS 5 也没人察觉；现在它们由机器断言。
  **CI 与交付前必须带上这个参数**，否则「浏览器那组全列 SKIP、退出码还是 0」这种假绿照样发生。
- **V6 负向测试是核心**：跑
  `python assets/negative_test.py <成品.html> --profile <profile.json> --img N`。
  每条判据都有对应注入，注入后核验器**必须 FAIL**。不能证明会失败的检查器等于没有检查——
  每次交付都做，不是做一次。
  本脚本末行还会判**注入覆盖率**：本次参数下跑得到的判据没有注入用例就 FAIL。
  V7 补上像素校验后，一条「把可见内容整体藏掉」的 CSS 注入就能让截图变纯色，
  HTML 注入动得了它了，所以它已回到注入路径——给 `--chrome` 时脚本自动备好 `--shot`。
  纯函数层的证明仍在 `check_demo.py --self-test`（纯色 / 半空白 / 正常 PNG 三个用例），单独跑一遍。
- **R1 元判据**：判据族脚本（check_demo / negative_test）自身不得出现产品分支；
  词表取 profile 的 product/vendor，或用 `--r1` 外供。词表为空时报 SKIP，不装绿。
- **D1–D5 落盘**：闸门通过 → 兜底存在（git 仓内 commit 且 `status --porcelain` 为空；
  非 git 则同 sha256 副本）→ 文件名匹配 `_v\d+\.\d+_\d{4}-\d{2}-\d{2}` →
  回读 size>0 且 sha256 一致 → 中文提交信息用 `-F` 传 UTF-8 文件并抓原始字节回读。
  复制过去之后跑 `python assets/check_demo.py <源件> --deliver <落盘件>`——
  除 D1 外这四条都已落成机器判据，不用靠人记。

任一门禁 FAIL → 回退上一环节并记录判据编号与原因；同一判据连续两次 FAIL → 停手回报，不打补丁硬推。

## 项目配置 `profiles/`

```
profiles/<产品>/
  profile.json     品牌色/字体/落点/素材接线/交互形态声明
  content.json     页面文案（标题 / 主张 / 页脚）
  sections.html    叙事五要件正文（带 data-role，交互件位置留 <!--INTERACTION-->）
  items.json       交互件数据源（能力型=样例，计算型=算例）
  *.md             该产品的口径纪律（红线、事实来源）
  *.png            素材图片，由 profile.assets 接线
profiles/_模板/    新建配置的起点（profile.json 字段骨架 + 新建子流程说明）
profiles/_示例/    随库入库的最小可复现闭环（全虚构值）——首次跑通与「本体零改动」复检都用它
```

下划线前缀 ＝ **基础设施**（模板、示例），随库入库；不带下划线的 `profiles/<产品>/` 是产品配置，
默认被 `.gitignore` 排除。所以**干净克隆里一定有一个能跑的完整配置**（`_示例/`），
不必先有真实产品才能验证本仓是否可用。

**本体（受保护集）** = `SKILL.md` + `assets/**`（含骨架、脚本、交互件模板）。
**本体零改动验收**：换另一个产品跑通全流程后，`git diff --stat -- SKILL.md assets/` 必须为 **0 行**；
有改动＝本体被项目特有能力污染，回退重构。

> **两个概念别混**：上面这个「本体」是**受保护集**（换产品不许动）；
> `audit_body.py` 的**扫描范围**比它更宽——技能目录下全部文本文件，排除 `profiles/` 与 `.git`，
> 连 README / CHANGELOG / manifest 一起扫。它们是会被发布出去的东西，冒出产品名同样算污染。
> 于是「本体零改动验收通过」与「清洁度扫描 0 命中」不是一回事，别拿一个的结论替另一个作答。

## 平台适配（检测到才启用）

- **WorkBuddy**：写入走 `enforce_contract.py --guard → 写 → --accept`。FAIL 时唯一合规处置是 `git commit` 建兜底，**禁止写后补 `--protect` 造假绿**。
- **其他平台**：落点目录可写即通过 D1，兜底用同 sha256 副本。

## 已知坑（实测，别再踩）

1. **跨会话要复用的中间产物禁止放 `%TEMP%`**——实测隔日被静默清零且无提示，
   必须当日落项目目录或 git 仓。（浏览器判据的**探针副本**属跑完即删的瞬时件，
   落在系统临时目录的一次性子目录里，不在此列——这不是自相矛盾，是两类东西。）
2. 图片先降采样再内联：base64 放大约 33%，一张 4MB 截图直接内联就爆 3MB 红线。
3. 数值逐位来自数据源，**禁止占位值**（示例/待定/TBD/XXX），核验做逐项比对。
4. headless 核验：`chrome --headless --dump-dom` 取 DOM、`--screenshot` 出图（Windows 上默认装在 `C:\Program Files\Google\Chrome\Application\chrome.exe`，用 `where chrome` 定位更省事）。无 tkinter，截图出文件后回读体积即可。
5. 含中文路径在部分检索工具下返回空，列目录用 PowerShell / Python。
6. **改判据必须同时配负向注入**——本轮靠负向测试抓到两个假绿：一是「注释掉的 `IX_init();` 仍被判为已调用」，二是「核验函数末尾的返回值没跟着改，而离线路径只走提前返回分支，所以看不出来」。判据写对了不等于写有效了。
7. **负向测试要带 `--chrome` 跑一遍**：离线路径与浏览器路径落在核验函数不同的返回分支上，只跑离线会漏掉浏览器侧的全部改动。
8. **`__pycache__` 不进库**（仓根 `.gitignore` 已配）：跑一次脚本就会生成，提交前确认 `git status` 里没有它。
9. **本体的产品词表不许写死**：`audit_body.py` 的基线词表为空，产品词一律从
   `profiles/*/profile.json` 自动收集（要屏蔽别的词用 `--words` 传外部词表）。
   写死产品名会让扫描器本身成为最后残留的那处产品名——检查定义与被检查内容必须分开。
   所以**不要**为了「让干净克隆也有判别力」而在仓里塞一份随库入库的词表：
   那等于把产品名写回本体，扫描器自己就成了本体里最后一处产品名。
   正确做法：词表放在仓外，用 `--words` 传；支持 `re:<正则>` 条目表达
   「代号 + 变体」这类形态。ASCII 词按「词边界 + 允许数字后缀」匹配
   （产品词最常见的出现形态是「代号 + 型号数字」，右边界含数字时这种形态一个都抓不到）。
   下划线目录（`_模板/` `_示例/` `_示例-带图/`）**不进词表**：它们是基础设施，
   词进去了反而会误伤自己造的示例。
   改完跑 `--self-test`：三族判据各注入一处故障，**产品词那族做"无词表 / 给词表"两跑对照**——
   给词表时必须命中（含型号后缀形态），无词表时必须不命中。判别力确实来自词表，才是真有效。
10. **改完判据要跑的不是一条命令，是四条**：`check_demo --expect`（数字契约）→
    `negative_test`（每条判据能失败 + 覆盖率）→ `check_demo --self-test`（注入动不了的那些）→
    `audit_body`。只跑第一条会在浏览器缺席时得到假绿，只跑第二条会漏掉 V7 这类判据。
11. **发布前跑 `python assets/release_check.py`**：版本号同时落在 `SKILL.md` frontmatter /
    `manifest.json` / `CHANGELOG` 三处，靠人记一定会对不上——而技能平台读的恰恰是
    `SKILL.md` 的 frontmatter。这条闸门就是为这次「1.2.0 的包装着 1.3.0 的内容」装的。
12. **新增下划线开头的示例目录时，`.gitignore` 的放行规则必须按模式写
    （`!profiles/_*/`），不要去枚举目录名**。枚举过一次就漏了：`profiles/_示例-带图/`
     被 `profiles/*` 悄悄排除，本机一切正常、干净克隆却少一整个示例，而发布闸门当时
     只查「文件在不在」照样报 PASS。现在 RC5 用 `git check-ignore` 守这一条。
13. **源仓改完 ≠ 用户用上了**：宿主加载的是运行态副本（`~/.workbuddy/skills/<技能名>/`），
    不是本仓工作树。改完必须同步过去，再跑 `python assets/release_check.py`——
    RC6 会逐文件比对两处，不一致即 FAIL 并点名差异文件（找不到副本时 SKIP 并说明，
    看不见不等于一致，不放绿）。
    代价实测过一次：副本落后 5 个 commit、48 份里 22 份内容不同，`release_check.py`
    在副本里整份不存在（不是版本旧，是根本没有）。
    同步（Windows）：`robocopy "<源仓>" "%USERPROFILE%\.workbuddy\skills\<技能名>" /E /XD .git __pycache__ .workbuddy 过程文件`
    ——用 `/E` 不用 `/MIR`：`/MIR` 会删掉副本里源仓没有的文件，而产品配置不入库，
    「只存在于副本」的内容（如某份配置的品牌色值）会被静默清掉。**同步前先备份副本。**

## 依赖

**无强制依赖**，只跑 Python 标准库。浏览器判据（`--chrome`）需本机有 Chromium 系浏览器，不给就单列 SKIP。

`cloud-design` **不是本仓依赖**——它只是维护者本机可选的一份额式参考（纯提示型、无脚本）。
本仓任何脚本、判据、出件流程都不引用它；未安装时出件与核验结果完全一致。
把它写在这里只为说明"版式灵感从哪来"，不是让你去装一个装不到的东西。
