# 安全策略

## 本仓是什么

一套**纯本地**的技能：出件器与判据脚本只在命令行给出的路径上读写，**不联网、不监听端口、
不安装任何依赖**（运行时只用 Python 标准库）。

唯一的外部进程调用是**可选的本机 Chrome / Chromium**——用于渲染层判据（无 console 报错、
横向溢出、截图完整性）。不提供浏览器时，这些判据列 `SKIP` 而不是假装通过。

## 数据处理

本仓不采集、不传输、不存储任何用户数据。没有服务端，没有遥测。

## 已知的文件系统行为

- **出件器**会把 `profile.assets` 指向的本地图片读出来、编码成 base64 内联进成品 HTML。
  因此：**不要把自己的配置指向不受信任的位置**。图片内容会原样进入成品。
- **浏览器判据**会写**三份**带探针的成品副本——错误捕获（B5）、溢出测量（V5）、
  交互往返（V8）。副本**优先落在系统临时目录**；拿不到临时目录时才退回到成品同级目录，
  跑完一律删除，**原成品文件一字不动**。所以这些判据需要一个可写的临时目录——
  完全只读的环境里它们会报错，不会假装通过。
- **负向测试**会在成品同级目录建一个 `_negtest/` 工作目录（临时目录不可用时同样退回），
  注入只改副本、不改原成品，跑完自清。
- **`extract_assets.py`** 会从既有单页 HTML 里抽内联图与数据到指定目录——同样只写你给的路径。

## 报告问题

发现安全问题时，请通过仓库 Issue 或私信维护者，不要先公开可直接利用的细节。

## 不在范围内

- 本仓生成的**演示成品**是对外展示物料，其中的数值与结论由使用者自己的产品配置决定。
  本仓不对配置内容的准确性负责——包括「数值是估算还是实测」这类口径问题，
  那是 `profiles/<产品>/` 自己的纪律（见该目录下的 `*.md`）。
