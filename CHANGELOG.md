# 变更记录

只记**已发生的变更**，不记计划。版本号同时落在 `manifest.json` 的 `version` 与
`SKILL.md` frontmatter 的 `version` 上——三处对齐由 `assets/release_check.py` 判，不靠人记。

---

## v1.8.5 — 2026-09-25

**定位纠偏（`description` + 「身份」新增「定位边界」），零行为变更。**

起因：此前 `description` 把「双击即开、能直接发微信、零外部依赖」作为卖点前置，
易让人误读本技能的差异化在「离线可发微信」。实际该属性是产出物的**必要属性、非差异化**——
任何单文件 HTML（含对话现做、沉浸滚动页）都能做到。

**改法**
- `description`：把「离线可发微信」从卖点降级为事实性产出属性，显式标注「任何单文件 HTML 均能做到，非本技能独占」；
  明确差异化落在三点——① 机器验证闸门（`check_demo.py` 逐项证明真离线，而非仅声称）
  ② 内容-版式分离、同视觉系统可复用 ③ 预制交互模块。
- 「身份」新增「定位边界（避免误读）」：写明必要属性 vs 差异化、代价
  （版式/动效预算约束、交互仅卡片/参数级、到不了真问答）、以及采用判据
  （高频 / 要一致 / 不容“假离线”才值；低频高度定制用「对话现做」更灵活）。

**边界**：本技能的价值是「带验证闸门的可复用对外演示件流水线」，不是「离线微信 HTML 工具」。

---

## v1.8.4 — 2026-09-24

**触发边界收紧（`description` + 「调用面」），零行为变更。**

起因是一次真实误用：一份 4.2 万字的个人经验长文被本技能接走，产出只保留源文 **39%**
（16,589 / 42,112 字），且 35 条正文被整体塞进 `data-role="evidence"`（实证）槽位——
在五要件结构里，正文从主角降格为论据。

根因不是执行质量，是**定位错配**——且它不是偶发误配，是本技能的**结构必然**（见下方「边界」）。
本技能的判定对象是「有主张要论证的对外演示」，那份内容的性质却是「呈现给人读」。
两类目的相反，于是两条核心规则同时反向生效：

- 叙事五要件（痛点 / 方法 / 实证 / 差异 / 边界）为说服服务，用在分享型内容上会强造出论证段；
- 版式预算（单区块 ≤120 字、单表 ≤8 行）为扫读服务，用在长文上变成硬性删减，
  被削得最狠的正是最长的几条（§31 剩 23%、§33 剩 30%、§35 剩 14%）。

**改法**
- `description`：删去「做个网页讲清楚 X」「出个能发给客户的页面」这两条无边界触发词，
  改以**有没有要论证的主张**为触发条件，并显式写出不适用场景与两条反向规则的后果。
- 「调用面」补「**先判性质，再看产品名**」——判据是受众看完要**做判断**还是要**读懂**。

**边界**：本仓只写「什么情况下不该用我」，不写「该用谁」——避免把另一个技能的名字焊进本体
（本体零改动纪律，见「项目配置」）。

**边界（结构层，同日取证补记）**：上方「定位错配」不是可修的缺陷，是**取舍的结果**。
`check_demo.py` 的 `NARRATIVE_ROLES` **强制**叙事五要件（缺件即 FAIL，连缺口都只能以
`data-role="gap"` 申报），交互件只允许四类模板。机制链条是：**能机检 ⇒ 判据锚在固定标记
⇒ 结构必须固定**。所以无论接什么题材，产物都先被改造成提案体，且只有一种骨架。
换骨架不解决问题——要「每条都能机检」就必须放弃「为内容量身设计结构」。
呈现型 / 长文型内容请直接用其他方式，**不要期待本技能换一副骨架**。
（独立对照件实测：同一份 4.2 万字长文，不套技能从零设计的一版保留源文 93%，
外链 0、390px 无横向溢出、单文件 170 KB——主观判「更爱读」。）

---

## v1.8.3 — 2026-09-20

修 **V7 在 Windows 上的失效根因**，并拆掉它赖以潜伏的温床。缺陷本身只有一行，
温床是「Windows 腿从来没跑过浏览器判据」——**修一行是治标，把覆盖补上才是治本**。

**修 ① 交给 Chrome 的路径不绝对化（`assets/check_demo.py`）**
- 症状：Windows 上 `--shot` 给相对路径时 Chrome 报 `Failed to write file …: 拒绝访问`，
  V7 判「截图未生成或为空」；同一命令换绝对路径即正常落盘（395 KB）。
- 修法：在 `chrome_checks` **入口单点**绝对化 `html_path` 与 `shot`，不回各调用处补。
  只在一处做，是为了下一个新加的参数不必再踩一次。
- **为什么此前没发现**：这条腿上的浏览器判据从未执行过。不是「写错了没人看见」，
  是「根本没跑」。同类问题的通解不是加注释，是让那个平台真的跑起来。

**修 ② 浏览器判据覆盖 Windows 腿（`.github/workflows/ci.yml`）**
- 依据：`actions/runner-images` 的 Windows2025 / Ubuntu 镜像清单均列有 Google Chrome
  （清单只给版本不给路径，所以路径靠枚举表，不在 YAML 里写死一个字符串）。
- 新增 `assets/find_chrome.py`。选它而不在 YAML 里写 shell 分支，三条理由：
  ① 枚举表能本地 `--list` 实测，shell 分支只能等 CI 告诉你；
  ② 写 `$GITHUB_ENV` 用 Python 的 UTF-8 无 BOM——PowerShell 5.1 的 `Out-File`/`Add-Content`
  默认带 BOM，键名会变成 `\ufeffCHROME`：变量静默不存在，判据整组 SKIP；
  ③ 两条腿共用一段逻辑，不存在「两条腿的探测条件不一样」这种只在 CI 上看得见的偏差。
- `--chrome` 改用 `${{ env.CHROME }}` 而非 `$CHROME`：后者在 bash 成立、在 pwsh 展开成空串，
  正好落进「整组 SKIP 且退出码仍为 0」那个形态。
- macOS 腿仍不纳入：一次引入「新平台 + 新路径」两个未实测变量，会把失败原因变成两种可能。

**验收（实跑，非推断）**
- 正向：本机 Windows、`--shot` 走**相对路径**、CWD 设在仓外——`_示例` 落盘 394509 字节、
  判定 29/0/2；`_示例-带图` 落盘 431304 字节、判定 30/0/1。与 `ci.yml` 的 `--expect` 逐项一致。
- 负向（证明这行是活的、且 V7 会拦）：拆掉 `shot` 的绝对化 → V7 当场报「截图未生成或为空」、
  判定 28/1/2、截图不落盘；复原后 sha256 与原件字节级一致（rb/wb 复原，不用文本模式）。
- 性质断言：`html_path` 那一行落在「探针注入失败才回落」的分支上，正常成品走不到，
  所以不靠端到端而靠注入式断言——把 `subprocess.run` 换成记录器，直接读 `--screenshot=`
  与全部 `file:///` 的实参，4 个 URL 与截图路径均为绝对路径。
  （依据：**没被执行过的分支等于未验证**，不能因为主流程全绿就认为它没问题。）
- 回归：三份随库示例离线 24/0/3、25/0/2、24/0/3（与 v1.7.0 记录一致，无漂移）；
  负向注入三份 rc=0；判据单测 18/18；本体清洁度 0 命中；RC1–RC7 全 PASS。
- **未闭合处 → 已闭合（23:16 回读）**：run 35518654946（`4f1f9be`）**六条腿全绿**。
  windows-latest 两条腿从「第 4 步就红、其后 16 步 skipped」变为 **20 步全绿**，
  其中第 17 步「确认浏览器可用」与第 18/19/20 步（浏览器判据含截图、带浏览器的负向注入）
  **success** —— 本轮唯一未实测的假设（runner 上 headless Chrome 的行为）由 runner 自己证实；
  macOS 两条腿按设计跳过浏览器那 4 步，其余 16 步绿。

**文档澄清 · 运行态副本的定位**
- 起因：宿主技能目录里遗留了一份 `.git`（技能目录搬迁时带过来的，HEAD 停在旧线），
  与源仓构成**两个同名仓**——同一技能名下两个仓，是「该往哪个仓 push」这类误判的根源，
  也让人误判运行态的版本状态。
- 处置：把那份 `.git` 移出运行态。迁移前逐项核对，它里面的一切在源仓都有：
  工作树 54 份逐字节一致、6 个提交全部包含在源仓历史内、5 个不可达 blob 源仓全部存在、无 stash。
- 验证：RC6 复跑 **54 份逐文件一致**；从运行态实际出件 `_示例` 得 24/0/3，
  与 `ci.yml` 的 `--expect` 逐项一致——**技能能力零影响**。
- `SKILL.md` 第 13 条同步补明：运行态是**纯文件副本，不是第二个仓**（不含 `.git`）。
- 顺带补上同步命令的一个缺口：`/XD` 名单漏了 `临时文件`（按工作区规范放的临时产物目录）。
  本轮同步实测把它带成了一个空目录（已清掉），补进排除表——否则该目录将来有内容时会被同步出去。

---

## v1.8.2 — 2026-09-20

修本仓 CI 在 GitHub 上的**首跑失败**（run `35516492259` / `e6a0ae1`）：6 条腿里
ubuntu / macOS 四条全绿，**windows 两条全红**，且都停在第一个 Python 步骤「发布闸门」，
其后 16 步 skipped。根因不在判据逻辑，在两个只在 Windows 成立的环境差异。本版把它们修掉，
并装了一条会 FAIL 的判据（RC7），让同类退步在本机就红，不必等 runner 告诉你。

**修 ① 输出编码（真凶）**
- 症状：`UnicodeEncodeError: 'charmap' codec can't encode characters`，退出码 1。
  runner 是 en-US，Python 的管道编码 cp1252 编不出中文，而结论行第一行就带中文。
- 本地以 `PYTHONIOENCODING=cp1252` 逐脚本复现：`release_check.py`、`audit_body.py`、
  `check_demo.py --self-test` **三个脚本退出码全部为 1**——整条 Windows 腿是结构性失败，
  不是某一步偶发。
- 修法：`ci.yml` 顶层加 `env: PYTHONIOENCODING: utf-8`，一处覆盖全部 Python 步骤，脚本一行未动。
- **为什么此前没发现**：本机是中文 Windows，控制台码页 cp936 能编码中文，于是
  「CI 本地等价首跑 14 步全绿」是**假绿**——被检验的性质（runner 的执行环境）与
  本地能观测到的性质（本机码页）根本不是同一个。与 V7 那次「截图只验文件存在、不验像素」
  是同一形态。

**修 ② shell 方言（潜伏的第二坑）**
- `ci.yml` 有 6 处 `run:` 用 bash 式反斜杠续行。Windows 上 `run:` 默认 shell 是 pwsh，
  反斜杠不是续行符——实测 `powershell -File` 直接
  `ParserError: 一元运算符"--"后面缺少表达式`，整个脚本解析失败。
- 修法：6 处全部折成单行（含 3 处仅 Linux 可达的）。规则零例外，RC7 因此不必再养一份
  「哪一步允许续行」的名单。
- **只修 ① 会在下一次 CI 立刻撞上它**：第 6 步「核验 · 示例 A 离线判据」正是续行写法。

**新增 RC7 · CI 配置自洽（`assets/release_check.py`）**
- 断言两条：顶层 `env` 声明 `PYTHONIOENCODING` **且值确为 utf-8**（只查键不查值会漏掉
  「写了但写错」，那又是假绿）；`run:` 块内不得有行尾反斜杠。
- **负向证明 4 项，逐项实跑**：① 删掉顶层 env 整块 → FAIL 且文案指出「未声明」；
  ② 值改 `cp1252` → FAIL 且指出实际值；③ 某步改回续行 → FAIL 并报出起始行号（第 65 行）；
  ④ 每次复原后回读 sha256 一致，基线回到 PASS。复原用脚本化 rb/wb——
  v1.8.1 的教训：文本模式写回会把 CRLF 改成 LF，「复原成功」会成为假象。
- 极限写在代码注释里：CI 的 env 与 shell 在本地没法真跑，本判据只能静态解析文件；
  它证明的是「配置层没有退回」，**不是「CI 真的绿了」**——后者只有真跑才算数。

**顺带**
- `RC3` 的 FAIL 文案把同一路径报两遍（`_negtest` 同时出现在两个来源），看着像两个残留物
  → 去重。判定行为未变。
- `README` / `CONTRIBUTING` / `SKILL.md` / `manifest.json`：RC1–RC6 → RC1–RC7；
  「CI 从未跑过」改为首跑事实；`missing_native_evidence` 里「Linux / macOS 未实测」已不成立
  （CI 四条 Unix 腿全绿）→ 移入 `native_tested`，并补上两处真实缺口。
- `README`「已知边界」新增：`--shot` 给相对路径时 Windows 上 Chrome 写不出截图
  （Chrome 报拒写文件；同命令给绝对路径即正常落盘，395 KB）。CI 不受影响——浏览器判据只在 Linux 腿跑。

**验收（本地实跑）**：三份示例离线 24/0/3、25/0/2、24/0/3（`--expect` 契约成立）；
浏览器档（`_示例` / `_示例-带图`）以绝对 `--shot` 复现 29/0/2、30/0/1，与 `ci.yml` 的
`--expect` 逐项一致；负向测试 29 条用例 0 问题（22 通过 7 跳过 / 24 通过 5 跳过 / 22 通过 7 跳过）；
CY 覆盖率 21 条判据条条有注入证明；判据单测 18/18；本体清洁度命中 0；
RC1—RC7 全 PASS（RC6 在同步运行态副本前为 FAIL，同步后转绿——这条 FAIL 形态本身就是闸门在工作）。

**未闭合**：Windows 腿的修复结果需推送后由 runner 复验；本机浏览器判据须给绝对 `--shot`。

## v1.8.1 — 2026-09-20

发布后回读发现的一批「陈述与事实不符」。全是文案与元数据，**判据行为一行没动**——
判据数字本身没漂移（与 v1.8.0 逐项一致），漂的是**描述它们的文字**。

**代码层一处（真隐患，不是文案）**
- `release_check.py` 的 RC6：报给人的副本路径与实际找过的路径**各拼一遍**——
  一处用 `os.path.basename(skill)`、一处留着没替换的 `<本技能名>` 占位符。
  于是「找不到副本」时 SKIP 文案指的路，根本不是刚才找过的那处路
  （同一事实两份来源——本仓已为这个形态付过三次学费：示例清单、文本口径、NEEDS_ARG）
- 改为探测路径只算一处，并做负向对照：传 `--runtime-dir C:/nonexistent_xyz` 时，
  SKIP 文案必须打出这个真实路径，而不是占位符

**示例 README 里的数字是过时的（被自己的判据点名）**
- `_示例/README.md` 仍写 `23/0/2`、`负向 26 条用例`，实测是 `24/0/3`、`29 条用例`——
  v1.7.0 新增 V9 后数字变了，**CI 里的 `--expect` 跟着改了，示例 README 没跟着改**。
  而这两份文档自己写着「对不上时改文档，别改实测」
- `_示例-带图/README.md` 同步回填：`24/0/1` → `25/0/2`，负向 `26 条` → `29 条`
- 两份的成品体积也由「约 1.4 万字节」改为实测的约 2.2 万字节
- 新增 `profiles/_示例-inline/README.md`：第三份随库示例、CI 对它断言了
  `pass=24,fail=0,skip=3`，仓内却没有一份文档记这些数字——另两份都有

**随库示例「两份」→「三份」**（v1.7.0 加了第三份，这些文案没跟上）
- `README.md` 4 处、`CONTRIBUTING.md` 3 处、`SKILL.md` 的 profiles 树与「不进词表」列举
- 顺带删掉 `README.md`「已知边界」里**重复出现两次**的同一条目

**一处陈述与 `.gitignore` 实际口径相反**
- `README.md` 原写「放行 `profiles/_模板/` 与 `profiles/_示例/` 两个」——实际是
  **按模式放行**（`!profiles/_*/`），随库的共 4 个。按名字枚举恰恰是本仓在
  SKILL.md 第 12 条明令禁止、且曾因此漏掉第二份示例的做法，陈述不该与规则相反

**发布元数据整块停留在发布前**
- `manifest.json`：`repository_url` 为空、`remote_repository_created: false`、
  `publishing_state: "local-only"`、`publish_blockers` 还写着「远端仓尚未创建」——
  而远端仓已建并推送（github.com/eiway112/product-demo，public / MIT / main）。按事实更新
- `README.md` 介绍发布闸门仍写「RC1–RC5」，实际是 RC1–RC6（RC6 查源仓 ↔ 运行态副本）

**验收**：三份示例离线 24/0/3、25/0/2、24/0/3；带 chrome 28/0/3、29/0/2、28/0/3；
带截图 29/0/2、30/0/1、29/0/2——与 v1.8.0 逐项一致，说明本版确实只动了文案；
负向 29 条用例 0 问题；单测 18/18；本体扫描 0 命中；RC1—RC6 全 PASS
（RC6 的 FAIL 形态亦在改前被实测抓到：刚改的 `release_check.py` 被判与副本不一致）。

## v1.8.0 — 2026-09-20

把「计算型」这个名字从**它做不到的事**上摘下来。改的是名字与形态定义，代码行为一行没动。

**命名与实现对齐（26 处文案）**
- 交互件自称由「推演器（参数 → 数值）」改为「**档位对照器（档位 → 数值）**」。
  实测确认：成品里 `input` / `select` / `textarea` / `form` 计数**全部为 0**，
  数值直接取自 `content.json` 的已实测数据集，页面内不做任何计算。
  原名字暗示「受众能改参数、看数值随之变」，而那是**另一种形态**，当前无模板
- 形态分类表的判定问句同步改：原问「受众能改参数、看数值随之变吗」——
  这个问句会把 ACE 一类产品直接判进计算型，然后给一个并不具备该能力的模板
- 文件名 `calculator.html` 与 `pattern` 值 `calculator` **一个字没动**：那是配置契约，
  动了所有既有配置当场失效。改的只是中文自称与定义
- `profiles/ace/profile.json` 的 `_why` 原本写「受众能改构造参数看数值随之变」——
  与模板实际能力不符，已改为「按构造档位分列对照」，并注明期望的真推演属待建形态

**边界写进契约（不再只是文案）**
- `assets/interaction_patterns.md` §二 新增「计算型的当前边界」：明写页面内不计算、
  无输入控件、实时推演属 §五 待建；并留一条防回潮判据——
  **判定问句里凡出现「能改参数」字样，一律视为命名回潮**
- 顺带记一条实测手法：验证「真的可交互」不能只看 DOM 变了，必须做**去监听对照**
  （把 `addEventListener("click", …)` 换注释、其余一字不动，再点一次必须一字不变）。
  少了这一步，「DOM 变了」可能是探针自己改的

**验收**：四份配置判据数字与 v1.7.0 逐项一致（`_示例` 24/0/3、`_示例-带图` 25/0/2、
`_示例-inline` 24/0/3、prefab-agent 26/0/1）；`--expect` 契约仍成立；负向 29 条用例 0 问题；
本体清洁度命中 0；RC1—RC6 全 PASS。改名未引起任何判据数字漂移。

## v1.7.0 — 2026-09-20

把「元判据自己也有静默通道」这一族收口，并给两条从未被覆盖的路径补上判据与输入。

**U1 出席扩面（D/U/CY）**
- 出席面从 24 项登记表扩到 D1—D5 与 U2，但是**条件性出席**：某一族本次根本没跑到
  （没给 `--deliver` / 没给 `--expect`）时要求它出结论，只会把原本 PASS 的路径判红——
  这正是「修一半让绿灯变红」的典型形态
- 顺序改了：出席判定移到落盘闸门**之后**，否则 D 系列的结论行还没产生就被判成缺席
- CY 同样要出席：它此前在「全覆盖」时一行都不出，与自己专门抓的静默消失同病
- D1 从「由调用方保证」落地成真判据：调用方把已判出的 FAIL 数传进来，上游还红着就不许落盘
- `delivery_checks` 的两条静默路径补上：落点不存在时 D2/D3/D5 报 SKIP 而非整段消失；
  非 git 落点此前一条 D5 都不出，现在显式报 SKIP
- 顺带修 D3：`_v<版本>_<日期>` 的正则只认两位版本号，`_v1.6.0_…` 被误判为不含版本

**两条新增判据（各配注入，负向已验）**
- **V9 关键数字回指**：成品数字带的每个数值必须在 `content.json` 的 stats 里有出处。
  数字带最容易被读成承诺，而此前它与 items 自洽与否只靠人眼。
  （编号跳过 V6——本仓已用「V6」指代 negative_test 这个脚本本身，判据号不与脚本代称撞名）
- **I4 装配位置**：`interaction.position` 的声明必须与成品里交互件实际所在次序一致。
  inline 分支此前在仓内**没有任何可跑输入**（四份配置无一声明），属于「有代码、从未被走过」；
  补了 `profiles/_示例-inline` 还不够——挪错位置必 FAIL 才算真的覆盖

**出件器**
- 声明 `interaction.pattern` 必须同时给出非空的 `_why`：四类形态只有两份模板，
  判错形态等于交互件不对题，没写依据的声明就是拍脑袋

**文档**
- `_模板/README.md` 里 `.py:<行号>` 形态的引用改成符号名/步骤描述——行号会漂，
  第三方评审报告点名 `check_demo.py:1096—1105` 时它已漂到 1110—1122，指认方自己也会漂
- 补一句「为什么不用 grep 机器化『新字段必须有消费点』」：字段名叫 `version`/`data`
  在代码里到处都是，命中不等于被读——那是假绿，比没有判据更危险
- README 补明 `--img N` 是契约不是提示（多一张少一张都 FAIL），此前示例照抄会踩坑

**数字同步**：新增判据后 `--expect` 全部重测并回填——`_示例` 离线 24/0/3、chrome 28/0/3、
带截图 29/0/2；`_示例-带图` 离线 25/0/2、带截图 30/0/1；负向测试 29 条用例（原 26）。
CI 同步加示例 C（inline）并更新四处的 `--expect`。

## v1.6.0 — 2026-09-20

主题：补上「改好了 ≠ 用上了」与「闸门红了 ≠ 抓对了」这两跳的常驻判据。
本轮改动全部来自第三方评审报告的**回读复核**（逐条取证，不采信报告结论，也不采信记忆），
每条都配负向测试——故意写错必须报错，否则分不清「判据有效」和「判据没在看」。

### A · 素材单源化（两份真实产品配置）

- `assets_dir` 不再指向运行态副本或别的项目资源目录，素材回到各自 profile 目录内（`""` = 本目录）。
  此前 `prefab-agent` 的素材取自运行态副本、`ace` 取自另一个活跃项目的专属资源目录——
  那份目录被改名 / 迁移 / 重装换掉时，成品里的图会静默变化，出件器唯一能做的只是打印一行提示。
- 同步前先抢救：`ace` 的品牌色值当时**只存在于运行态副本里**（源仓 `content.json` 未建），
  覆盖即永久丢失——已回写源仓并注明它不生效、等 `content.json` 建好后迁移。
- `profiles/_模板/README.md` 补明 `assets_dir` 留空的语义，并写明不要指向别的项目目录。

### B · RC6：源仓 ↔ 运行态副本一致（`assets/release_check.py`）

宿主真正加载的是 `~/.workbuddy/skills/<技能名>/`，不是源仓工作树；这两处之间的搬运此前靠人记得。
实测：副本落后 5 个 commit、48 份里 22 份内容不同、`release_check.py` 在副本里**整份不存在**。
RC6 逐文件比对两处，不一致即 FAIL 并点名差异；副本找不到时 **SKIP 并说明**（看不见不等于一致，
不许静默放绿）。支持 `--runtime-dir` 显式指定。
- 负向测试：改副本一个字节 → RC6 FAIL 并点名该文件；复原 → PASS。
- **首跑即抓到真实差异**（RC6 自身还没同步过去）——这条不是人为造出来的。

### C · 清洁度口径收口（`assets/audit_body.py`）

`.gitignore` 排除 `过程文件/` 并注明「与 `.workbuddy/` 同一口径、不算本体」，而扫描器的 `SKIP_DIRS`
里没有它——注释写完当天就漂移了，于是清洁度闸门在本工作树**永久假红**（基线命中 2，
红的原因与「本体被产品特化」毫无关系）。永久红的闸门等于没有闸门。
- 修法不是补一个目录名（那会重演）：新增**对齐判据**——`.gitignore` 的顶层目录排除项
  必须在本扫描器的跳过集合内，漏一个就点名；`__pycache__` 一类技术产物目录单独豁免，避免无意义假红。
- 基线由「命中 2」回到 **命中 0**；负向：临时加 `临时文件/` → FAIL 点名，复原 → PASS。

### D · 对外证据面收口（`manifest.json`）

`native_tested` 的 scope / evidence 曾把 `profiles/prefab-agent`（真实产品配置）写成能力证据，
而该配置被 `.gitignore` 排除、干净克隆里根本没有——**对外声明指向一个别人拿不到的东西**。
数字已移入新增的 `local_only_native_evidence`，显式标注「本机私有验证、配置不随库发布、不可复现」。
「换产品时本体零改动」这条能力由两份随库示例（计算型 + 能力型）承载，不依赖它；
v1.5.0 记录的「跨产品验收」同属本机私有、同样不可复现（历史条目不改写，在此说明）。

### 实测

| 对象 | 结果 |
|---|---|
| `_示例`（离线，带 `--img 0`） | `PASS 23 / FAIL 0 / SKIP 2`，U2 期望契约 PASS |
| `_示例-带图`（离线，带 `--img 2`） | `PASS 24 / FAIL 0 / SKIP 1`，U2 期望契约 PASS |
| 负向测试 `_示例`（离线） | 26 用例：通过 20 / 问题 0 / 跳过 6 |
| `check_demo --self-test` | 18 条全部符合预期 |
| `release_check.py` | RC1–RC6 全 PASS（RC6：48 份逐文件一致） |
| `audit_body.py` | 命中 0；自检三族各自被证明会失败 |

---

## v1.5.0 — 2026-09-20

触发：真实成品**人工读图**判定「像说明书，没有 demo 感」——机器判据全绿，语义级判据只有读图能抓。
主题：成品观感从「白皮书」升级为「产品演示」。本轮 = 方案 A（骨架视觉动效升级）+ 方案 B（交互件前置）；
不做方案 C（滚动叙事档位，留 v2.0）。

### A · 骨架视觉动效升级（`template_skeleton.html`）

- **首屏 hero 化**：品牌色深渐变底（只取 `brand-dark → brand` 两级——两级都是中深色，
  白字在任何品牌配色下都读得清，不赌浅色端）+ 光晕 + 大标题（强调词改「荧光笔下划」高亮，
  不再用强调色换字色——换色在深底上要碰运气）+ 白色胶囊 CTA + 底部滚动引导箭头（纯 CSS 动画）。
- **关键数字区（可选）**：`content.json` 新增可选 `stats`，出件器按它生成「大数字 + 标签 + 归一条形」
  卡片带；数字滚动到终值、条形长到终宽。数字终值与条形终宽**写进标签与行内样式**——
  脚本不跑时它们本来就是终态（沿用 v1.4.1 的首屏门控纪律：动画只是加强，不是内容的承载途径）。
- **卡片 hover**：交互件卡片悬停抬升 + 投影（两份交互件模板同步改）；`.on` 写在 `:hover` 之后，
  选中态配色不被悬停态盖掉。
- **分节渐入 stagger**：分节与关键数字卡入场错峰（同批进入视口的按序各延 80ms）。

### B · 交互件前置（`build_demo.py`）

- profile 新增 **`interaction.position`**（默认 `front`）：交互件提到所有分节之前——
  叙事从「先读完再玩」改成「先玩后读」，hero 的 CTA「先玩一下」直达；`inline` 保持旧装配
  （按 sections 里的标记位插）。front 模式下 `<!--INTERACTION-->` 标记仍必须存在
  （出件器靠它确认装配点已声明，丢了会中止而不是静默）。
- 骨架新增 `<!--PLAY-->` 与 `<!--STATS-->` 两个可选块：无交互件时 CTA 整块移除
  （没有交互件却留着「先玩一下」＝点了没地方可玩），无 `stats` 时数字带整块移除。

### 进场动效的兜底（本轮两次实测翻车都是环境时序，不是页面逻辑）

- **headless 截图 / 虚拟时间环境的实测时序**：脚本**求值期注册的定时器不触发，load 之后注册的才触发**
  （V8/B5 探针全是 load 后注册，所以此前从未暴露）；IO 回调不可靠；rAF 极慢
  （虚拟预算 4000 只够 900ms 计数走约 8%）。进场显形因此改为三通道：
  IO 优先 + load 后定时视口扫描（150/500/1000/2000ms）+ scroll/resize 扫描；
  rAF 计数另加 1200ms 落定兜底。任何一条通道失效，内容照样显形、数字照样落到终值。
- **V7 改用「动画冻结」副本截图**（`check_demo.py` 新增 `V7_FREEZE`）：冻结转场 + 把
  `data-count` / `data-grow` 钉到终值并**摘掉属性**（不摘的话，骨架 load+150ms 起跳的
  计数动画会把钉好的值覆盖回去）。V7 判的是「内容在场 + 画面非空白」，不是动画时序——
  实测同一成品 budget 4000/12000/15000/20000 四次截图显形状态互相矛盾（其中两张字节级相同），
  调预算救不了，冻结才确定。附带收益：`.fx` 若没有 `.js` 门控（v1.4.1 的 A 缺陷形态），
  冻结 CSS 只放行带门控的隐藏，无门控的 `.fx` 依旧隐藏——**V7 从此也拦那一类缺陷**。

### 数字与契约（全部实测复核）

- `--expect` 契约**全部不变**：`_示例` 离线 23/0/2、chrome 27/0/2、+shot 28/0/1；
  `_示例-带图` 离线 24/0/1、+shot 29/0/0；`prefab-agent`（真实产品）离线 24/0/1、+shot 29/0/0。
- 负向测试 26 条用例全部被拦：`_示例` 离线 20 通过/6 跳过、chrome 24/2；
  `_示例-带图` 离线 22/4、chrome 26/0；`prefab-agent` chrome 26/0。
- 判据单测 18/18；本体清洁度命中 0。
- 配置文档同步：`profiles/_模板/README.md` 增补 `interaction.position` 与 `stats` 的消费点与规矩
  （`value` 必须数值型、每个数字必须能回指出处，禁止为好看凑数）。

---

## v1.4.1 — 2026-09-20

触发：真实产品配置（`profiles/prefab-agent/`）验证时**人工读图**发现两处缺陷——
机器判据全部全绿，页面却可能给客户看到空白首屏。主题：把这两类风险修在根上，
并让判据看得见画面。

### 缺陷 A · 首屏内容可见性无条件依赖脚本（`template_skeleton.html`）

- `.fx{opacity:0}` 无条件隐藏首屏标题 / 引导语 / 主图，唯一显形途径是脚本加 `.in`。
  实测：摘掉全部 `<script>` 后等 3 秒截图，首屏整片只剩 hero 渐变底——
  而所有静态与浏览器判据照常全绿。成品的承诺是「能直接发微信」，微信内置 WebView
  对脚本与 IntersectionObserver 的行为不保证与实测 Chrome 一致。
- 修法：`.js` 门控——脚本首行给 `<html>` 打标记，CSS 改为 `.js .fx{…}`；
  IntersectionObserver 不可用时直接显形。脚本跑不起来时内容本来就在：
  **宁可没有动画，不能没有首屏。**
- 边界（如实说）：这修掉的是「脚本不可用 → 首屏空白」这个已知脆弱点，
  不等于验证过微信真机。

### 缺陷 B · V7 截图判据是盲的（`check_demo.py`）

- 旧 V7 只验「文件存在 + 非空 + PNG 头 + 尺寸」，不看像素——这四样对一张全白页全部成立；
  且 V7 被登记进 `UNIT_PROVEN` 豁免，「永远拿不到会失败的证据」。
  这正是本仓最反对的「检查器 ≠ 它声称守护的性质」的教科书形态。
- 修法：新增 `png_pixels_ok()`——纯 stdlib（zlib + struct）解 PNG、逐行反滤波、
  纵向 4 段逐段数颜色，任一段 < 8 种颜色判空白。阈值依据（2026-09-20 实测，
  1080x2400、每 4 行/每 4 列采样）：三份真实成品每段 116–343 种颜色，纯色页 1–3 种，
  余量两个数量级。
- V7 拉出 `UNIT_PROVEN`（名单置空），`negative_test.py` 新增「把可见内容整体藏掉」的
  CSS 注入，并自动备好 `--shot`——给 `--chrome` 时 V7 每轮都真跑、注入后必须 FAIL。
  `--self-test` 保留纯函数层证明（纯色 / 半空白 / 正常 PNG 三个用例），18 条用例全过。
- **判据边界要说清**：像素校验守的是「整页非纯色」，抓不到「只有首屏空白、下方正文
  照常渲染」的 A 形态——实测该形态各段颜色数与正常页同量级。A 缺陷目前靠人工读图守，
  是否加一条源码层判据（`.fx` 隐藏必须带 `.js` 门控）待拍板。
- 顺带：`f_nested_script` 改为锚定含 `IX_ITEMS` 的脚本段——骨架在 `<head>` 新增了
  首屏门控脚本，「第一个 `<script>`」不再是交互件脚本，旧锚会注错地方。

### 数字变化（全部实测复核）

- `check_demo` 的 `--expect` 契约**全部不变**（23/27/28、24/29 等逐一实测一致），
  V7 结论行从「截图完整」变为「截图完整且画面非空白（4 段每段 ≥N 种颜色）」。
- 负向测试：`_示例` 离线 21 通过/5 跳过 → **20 通过/6 跳过**（新增的 V7 注入离线列
  SKIP）；浏览器侧 24 通过/2 跳过、`_示例-带图` 26 通过/0 跳过——总用例数 26 不变
  （多一条 V7 注入，少一条 CY 的单测证明行）。
- `audit_body` 排除表补 `.workbuddy/`（智能体会话运行态，未入库不发布）——
  实测会话记忆文件里的产品词曾把整条防线判 FAIL。

---

## v1.4.0 — 2026-09-19

本轮整改的触发是一份第三方评审报告；主题是**把「逐条补漏洞」改成「让漏洞这一类无法存在」**。
整改前先逐条复现了报告里的 7 组对抗性探针（全部复现），并额外查到 3 处报告没提的同类缺陷。
下面每一条都是实测出来的，不是推断的。

### 架构层（治根，不是补漏）

- **判据登记表 + U1 出席检查**。所有判据号登记进 `check_demo.py` 的 `CRITERIA`，
  跑完逐条对照：没出结论行的直接 FAIL。此前「静默消失」发生过三次（V3/V7 缺参数、
  V2 在成品无图时一行不出），每次都是单独补一条——打地鼠。V2 那条是本轮新查到的：
  `_示例` 不配图，于是 V2 在默认跑法下**一行结论都不出**，「图片验过了」是读出来的假象。
  现在它列 SKIP，离线跑法的数字由 `PASS 23 / SKIP 1` 变成 `PASS 23 / SKIP 2`。
- **U2 期望契约（`--expect pass=n,fail=n,skip=n`）**。文档里的那些数字此前没有任何东西在看。
  现在它们由机器断言，CI 与交付前带上这个参数即可——「浏览器那组因 chrome 路径失效全部列 SKIP、
  退出码还是 0」这种假绿（评审报告 H4）不再可能发生。
- **注入覆盖率变成机器判据（CY）**。`CONTRIBUTING` 第一条「每条判据必须能被证明会失败」
  此前只写在文档里，于是 B5 与 V7 长期没有注入而无人发现（H2）。现在负向测试末行逐条点名
  缺用例的判据并 FAIL；V7 这类「注入动不了它」的，会去真跑 `check_demo --self-test` 证明。
  顺带查到 V1 也从未被点名（它与 B1 共用一行，期望里只写了 B1）。
- **共用口径收敛到单一实现**。文本口径（`visible_text` / 标记层文本 / 剥注释）与词条匹配
  （`word_hit`）此前各有两份实现，一边剥 HTML 注释、一边不剥，两边注释都写着「同口径」。
  全部收敛进 `measure_density.py`，`check_demo.py` 与 `audit_body.py` 都从那里取。

### 判据能力

- **新增 V8 · 交互往返**（本轮最大的能力台阶）。派发一次真实点击 → 回读 DOM →
  与数据源的第 k 项逐项比对。此前没有任何一条判据派发过点击事件（H3）：
  卡片索引错位、事件绑定失效、面板只更新一半三类故障在所有静态判据下全绿。
  配套新增契约 **C7**（卡片须为 `.ix-card` 且带 `data-ix` 索引，主标题容器 `id="ix-title"`）。
- **B1/V1 扩到 CSS 层**：`url()` / `@import` / `srcset` 一并判（M5）。只扫 src/href 属性时，
  一条 `@import url("//…")` 就能让「离线自包含」整条失效而判据仍报绿。
- **B6 先剥 URL 再匹配**（M6）。旧正则把 `https://` 里的 `s:/` 当成盘符路径，
  正文里放一句普通网址就假红；假红把这条判据的真实语义淹掉了。顺带补上 UNC 路径的识别。
- **I2 剥全部注释形态**（M7）。此前只剥行注释，块注释版注入照样报绿——
  而负向注入用的恰好也是行注释，两者共用同一个盲区。补一条块注释版注入。
- **check_demo 单测模式（`--self-test`）**：15 条纯函数用例，覆盖 V7（PNG 头/尺寸/空/不存在）、
  I2（剥注释且不误伤字符串与 URL）、B1（CSS 外链与 srcset）、B6（URL 与盘符、UNC）。
  这些断言嵌在浏览器流程里永远无法被注入证明会失败，只能从函数层证伪。

### 出件器与落盘

- **`build_demo.py` 形状校验**：JSON 合法但形状不符（`items.json` 写成顶层数组、
  `content.json` 缺 `brand`）此前是 AttributeError / KeyError 裸回溯（M4 及同类）。
  统一走一条形状校验，给「出件中止：…」+ 具体路径 + 退出码 2。
- **素材取数可回读**（M10）：相对路径按 profile 所在目录解析（此前按当前工作目录解析，
  换个目录跑拿到的是另一份图），并打印每张素材的绝对路径 + sha256 前 12 位；
  素材取自 profile 目录之外时额外提示。
- **探针副本改 `mkdtemp`**（M12）：固定文件名落在系统临时目录，并发两跑或中断残留会互相覆盖。
- **D1–D5 落成机器判据**：`--deliver <落盘件>` 判文件名版本日期、sha256 一致性、
  git 兜底（或同 sha256 副本）、提交信息编码可读。

### 新增

- **第二份示例配置 `profiles/_示例-带图/`**（catalog 形态 + 两张极小的 PNG）。
  它补两个盲区：B2/V2 在干净克隆里永远列 SKIP（没图可判）；
  catalog 模板的 ✅ 此前只有维护者本机能核。现在两处任何人可复现。
- **`assets/release_check.py`**：发布闸门。第一条就是版本三处一致——
  评审报告 H1 指出 `SKILL.md` 写着 1.2.0 而 manifest 是 1.3.0，而发布前清单里
  没有任何一条会拦住它。这条闸门现在会 FAIL 到那次为止的所有同类状态。
- **RC5 入库可见性**（本轮自查新增，报告未提）。RC4 只证明「示例文件在磁盘上」，
  而干净克隆拿到的是「git 认为该入库的那些」。第二份示例 `_示例-带图/` 就被
  `.gitignore` 悄悄排除了——放行规则枚举了 `_模板` / `_示例` 两个名字，与
  「下划线开头＝基础设施」这条约定不一致。已改回按模式放行（`!profiles/_*/`），
  并给闸门补一条 `git check-ignore` 取证面；负向验证过：退回枚举式写法时 RC5 会 FAIL。

### 修复（文档与实现对齐）

- `SKILL.md` frontmatter 版本 1.2.0 → **1.4.0**（三处对齐）。
- 「四步流水线」两个含义分开写（M1）：`SKILL.md` 是业务流程四步，README 是四道机器闸门。
- 修掉两处「本仓尚未配置 CI」（M2）——同一份 README 的「已知边界」写着 CI 配置已写入。
- `interaction_patterns.md` §三 C6 与 CHANGELOG 关于「写成完整字面量时看起来仍然是绿的」
  一句与实现相反：实现是**显式 FAIL**（安全方向）。按实现改文档（同 L4）。
- `profiles/_模板/README.md` 字段表拆成「机器消费」与「仅约定」两列（M3）：
  `vendor` 在出件链路上从未被读取，`delivery_dir` / `audience_default` / `form_default`
  是纯约定；`product` 只进词表，页头页脚实际取自 `content.json`。
- 明确「本体（受保护集）」与「清洁度扫描范围」是两个概念（M8）。
- 两份真实产品配置里的 `brand` 字段：prefab-agent 已删（配色在 content.json 有同源记录）；
  ace 的 content.json 尚未建，暂留并标注原因。
- `.gitignore` 的放行规则**按模式写，不枚举目录名**（见「新增」的 RC5）。
- CI（`.github/workflows/ci.yml`）逐步带上 `--expect` 断言，并纳入第二份示例、发布闸门
  与 `check_demo --self-test`；浏览器步骤前先断言 chrome 存在，避免「找不到浏览器」被误读成
  「判据通过」。该工作流**仍未在 GitHub 上真实跑过**，这一点写在文件头注释与 README 已知边界。
- `SECURITY.md` 的探针副本数量与落点按实现更正（三份探针、优先系统临时目录、
  临时目录不可用时退回成品同级，跑完自清）。

### 验证记录（本轮实测，非推断）

| 对象 | 结果 |
|---|---|
| `_示例`（无图）离线 / 带浏览器 / 带截图 | `PASS 23 / SKIP 2` → `PASS 27 / SKIP 2` → `PASS 28 / SKIP 1` |
| `_示例-带图`（catalog + 2 图）离线 / 带浏览器+截图 | `PASS 24 / SKIP 1` → `PASS 29 / SKIP 0` |
| 负向测试 `_示例` 离线 / 带浏览器 | 26 用例：通过 21 / 跳过 5 → 通过 24 / 跳过 2 |
| `check_demo --self-test` | 15 条用例全部符合预期 |
| 跨产品 `profiles/prefab-agent/` | `PASS 28 / FAIL 0 / SKIP 1`，本体（`SKILL.md` + `assets/**`）零改动 |
| `release_check.py` | RC1–RC5 全 PASS；RC5 负向验证会 FAIL |
| `audit_body.py` | 命中 0；自检三族各自被证明会失败 |

---

## v1.3.0 — 2026-09-19

本轮整改的主题是**把「检查器声称守护的性质」与「它实际守护的性质」对齐**。
以下每一条都是实测出来的，不是推断的。

### 新增

- **交互件契约 C6**：模板必须在 `IX_init()` 调用**返回之后**，向 `document.documentElement`
  写入 `data-ix-ready` 取证锚点，值用拼接写法（`'IX' + 'RDY'`，运行时得 `IXRDY`）。
  没有它，行为层判据 V3（「JS 真的执行了」）在本技能里**永远只能列 SKIP**——因为本技能
  强制数据内联（B1/V1 要求零外部引用），「在 DOM、不在源码」的值本来就不存在。
  值必须拼接：写成完整字面量会让 V3 当场失效，而它看起来仍然是绿的。
  > **勘误（v1.4.0）**：后半句与实现相反。实现是**显式 FAIL**（报「注入值在原始源码中已存在」），
  > 属安全方向、不会放绿；真实代价是这条判据失去取证力。保留原文是为了不改历史记录，
  > 正确表述见 v1.4.0 修复条目与 `interaction_patterns.md` §三 C6。

### 修复

- **B5 名实不符**。文档写的是「headless 无 console error」，实现只判 `dump-dom` 返回非空。
  现改为在副本的 `<head>` 之后注入捕获器（必须最早执行；快照放在 `load` 之后 1.5 s，
  否则读不到异步抛出的错误），真判 `console.error` / 未捕获异常 / `unhandledrejection`。
  之所以不用 Chrome 自己的日志：`--enable-logging` 把 `log` / `warn` / `error`
  一律报成 `INFO:CONSOLE`，分不出级别。
- **V3 / V7 缺参数时整段静默消失**。既不 PASS 也不 SKIP，于是「没查」被读成「查过且干净」，
  与核验器自己声明的三态语义直接冲突。现列 SKIP，并在汇总行逐条列出。
- **负向测试的多值期望是交集判定**。`expect='I2,V3'` 时只要 `I2` 命中就报 PASS，
  `V3` 从未被验证也看不出来——报告上还写着「期望 I2,V3」。现改为并发判定：
  每条期望都要命中；本次调用参数下跑不到的，在结论里点名「V3 未验证：--js-value」。
- **`audit_body.py` 结构签名的正则灾难性回溯**。两条 lookahead 串联的正则
  `(?s)(?=.*A)(?=.*B)(?=.*C)` 在 `(?s)` 下每个起始位置都要扫到文本末尾，退化为 O(n²)：
  23 KB 文件单条 7.24 s。改为三个词各自按词边界独立判定（语义等价）后 0.001 s，
  全仓 33.8 s → 0.08 s。等价性经 11 文件 × 2 条签名逐一比对，结论完全一致。
- **`profiles/_模板/profile.json` 里删掉从不生效的 `brand` 字段**。品牌色只从 `content.json` 取；
  留着它会让配置者以为改这里有效。出件器现在遇到该字段会打印提示而不是静默忽略。
- **`build_demo.py` 读配置失败时抛裸回溯**。`--profile` 传目录
  （`--profile profiles/_示例`）得到的是 `PermissionError` 的 Python 回溯；配置不存在、
  或 JSON 写坏，同样是裸回溯。可这个参数名看着就像「产品目录」，首次使用者几乎必踩；
  而回溯只说明 Python 崩了，不说明该改哪里。现统一走 `load_json()`：
  三例都给出「出件中止：…」+ 具体路径 + 退出码 2。`check_demo.py` / `audit_body.py`
  本就如此，这次补的是三处一致。

### 变更

- **V7** 除「非空」外校验 PNG 头与尺寸；校验逻辑抽成 `png_head_ok()` 以便单测
  （直接嵌在浏览器流程里，Chrome 正常时永远产出合法 PNG，这条断言就无法被注入证明会失败）。
- **`negative_test.py` 注入总数 20 → 21**：「摘掉 `IX_init` 调用」的期望由 `'I2,V3'` 改为 `'I2'`
  （脚本其余部分照跑，V3 本不该被触发），多值用例改由新增的「整个交互件脚本被掏空」承担。
- **`profiles/_示例/` 随库入库**：一份完整可跑的最小配置（全虚构值），用于首次跑通、
  干净克隆自检、以及「换产品本体零改动」这条验收的**可复现输入**。
  此前该验收只在维护者本机成立——因为配置不入库。
- 骨架的可选素材机制：`<!--ASSET:logo-->` / `<!--ASSET:hero-->` 未提供素材时**整块移除**。
  此前 logo/hero 是硬编码固定元素，**没有图的产品根本出不了件**（空指针直接中止）。
- `.gitattributes` 钉住 LF；`.gitignore` 明确「下划线前缀＝基础设施，随库入库」。

### 文档

- 契约号补到 C6，并在四处（`interaction_patterns.md` / `check_demo.py` / `SKILL.md` / `README.md`）同步。
- 删除全部「CI 会自动验证」式表述——本仓当时还没有 CI。缺口登记进「已知边界」。
- `profiles/_示例/README.md` 给出每一步的**预期输出**，且这些数字与实测逐项对齐。

---

## v1.2.0 — 公开首版

更早的版本没有留下记录，本文件自 v1.3.0 起维护。
