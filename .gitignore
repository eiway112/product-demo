# ── 产品配置：含商业资产，不入公开仓 ──────────────────────────────
# 采用「默认排除、显式放行」：新增产品配置自动被排除，不必记得回来改这里
# （靠规则拦截，不靠自觉）。被排除不影响本地运行——版本控制与运行是两回事。
# 约定：下划线开头的目录是「基础设施」（模板、示例），不是产品配置；产品配置不带下划线。
# 放行必须**按模式**写，不能枚举目录名——枚举过一次就漏了：新增的第二份示例
# `profiles/_示例-带图/` 被 `profiles/*` 悄悄排除，干净克隆里根本没有它，
# 而发布闸门只看「文件在不在」，不看「会不会入库」，于是照样报 PASS。
profiles/*
!profiles/_*/
!profiles/_*/**

# ── 字节码缓存（py_compile / import 产生）────────────────────────
__pycache__/
*.pyc

# ── 会话运行态：宿主管理的会话记忆，永不发布 ─────────────────────
# 与 audit_body.py 的 SKIP_DIRS 同一口径：既不进公开仓，也不算本体。
# 不排除会让 D2「工作树不干净」永久 FAIL——它不是本体改动，是运行痕迹。
.workbuddy/

# ── 过程产物：含本机路径与内部治理语汇，不入公开仓 ───────────────
# 与 .workbuddy/ 同一口径：既不进公开仓，也不算本体。
# 兜底由 02_成果交付库 的 git 历史承担（归位前已入库）。
过程文件/
